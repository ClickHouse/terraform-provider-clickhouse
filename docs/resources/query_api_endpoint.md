---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "clickhouse_query_api_endpoint Resource - clickhouse"
subcategory: "ClickHouse Cloud"
description: |-
  Use this resource to create a Query API endpoint from a SQL query in ClickHouse Cloud. The endpoint runs the query over HTTP, so callers do not need a native driver to connect to the ClickHouse Cloud service. You can configure query parameters, API keys, database roles, and CORS allowed domains.
  For more information, see Setting up query API endpoints https://clickhouse.com/docs/products/cloud/guides/sql-console/query-endpoints.
  ~> Note: This resource is in beta. Its behavior may change in future provider versions.
  You can import an existing endpoint if it was created through the Query API endpoints API.
---

# clickhouse_query_api_endpoint (Resource)

Use this resource to create a Query API endpoint from a SQL query in ClickHouse Cloud. The endpoint runs the query over HTTP, so callers do not need a native driver to connect to the ClickHouse Cloud service. You can configure query parameters, API keys, database roles, and CORS allowed domains.

For more information, see [Setting up query API endpoints](https://clickhouse.com/docs/products/cloud/guides/sql-console/query-endpoints).

~> **Note:** This resource is in beta. Its behavior may change in future provider versions.

You can import an existing endpoint if it was created through the Query API endpoints API.

## Example Usage

```terraform
variable "service_id" {
  description = "ID of an existing ClickHouse Cloud service."
  type        = string
}

data "clickhouse_api_key_id" "current" {}

resource "clickhouse_query_api_endpoint" "format_date" {
  service_id = var.service_id

  name     = "format-date"
  sql      = "SELECT formatDateTime({date:Date}, '%F') AS date"
  database = "default"
  parameters = {
    date = "2026-08-13"
  }

  api_key_ids     = [data.clickhouse_api_key_id.current.id]
  roles           = ["sql_console_read_only"]
  allowed_origins = ["https://example.com"]
}

output "query_api_endpoint_url" {
  description = "Public URL of the Query API endpoint."
  value       = clickhouse_query_api_endpoint.format_date.url
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `api_key_ids` (Set of String) API key IDs allowed to call the endpoint.
- `database` (String) Database used by the Query API endpoint.
- `name` (String) Name of the Query API endpoint.
- `roles` (Set of String) Database roles used to run the query.
- `service_id` (String) ID of the ClickHouse Cloud service that runs the query.
- `sql` (String) SQL executed by the endpoint.

### Optional

- `allowed_origins` (Set of String) Origins allowed by the endpoint CORS policy. Omit this field to restrict access to backend servers.
- `parameters` (Map of String) Default query parameters.

### Read-Only

- `id` (String) Unique ID of the Query API endpoint.
- `url` (String) Public URL used to execute the endpoint.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/bash
# Import by service_id:endpoint_id.
terraform import clickhouse_query_api_endpoint.format_date 22222222-2222-2222-2222-222222222222:33333333-3333-3333-3333-333333333333
```
