.terraform*
terraform.tfstate*

terraform-provider-clickhouse

node_modules/
secrets.tfvars
dev/
.idea/
*.backup

**/variables*.tfvars

/tmp

# Build and test artifacts (make goreleaser-check / make cover)
dist/
cover.out
cover.html

# UDF source archive zipped by the archive provider at plan time
examples/full/udf/*/build/

# Credential files: .env-e2e, demos/*/.env. None are tracked, so ignore the glob.
.env*
